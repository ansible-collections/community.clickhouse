secret_hidden: true
expected_no_log:
  partial: "{{ '********' if secret_hidden else secret }}"
  secret: "{{ 'VALUE_SPECIFIED_IN_NO_LOG_PARAMETER' if secret_hidden else secret }}"
  secret2: "{{ 'VALUE_SPECIFIED_IN_NO_LOG_PARAMETER' if secret_hidden else secret2 }}"
